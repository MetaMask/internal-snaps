{
  "extends": "../../tsconfig.packages.libs.build.json",
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist",
    "rootDir": "./src"
  },
  /**
   * Unlike `tsconfig.json`, for builds, we only include the source code.
   */
  "include": ["./src"]
}
