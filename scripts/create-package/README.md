# `create-package`

This directory contains our CLI for creating new monorepo packages.
For details, please see [the monorepo contributor documentation](../../docs/processes/adding-new-packages.md).

Library packages are created by default. Pass `--type snap` and
`--proposed-name "My Snap"` to create a Snap package.
